# تعليمة بناء · منصة اللقاءات العلمية — جمعية سنن

هذه الوثيقة مستقلة: كل ما تحتاجه للبناء مذكور فيها. لا تعتمد على محادثة سابقة.

---

## ١ · ما تبنيه

موقع عام يعرض جدول اللقاءات العلمية في جمعية سنن، مع لوحة تحكم داخلية لمحرّر واحد.
اللغة عربية RTL حصراً. التقويم المعروض هجري. الأوقات كلها بتوقيت السعودية.

**الأدوار:**
- **الزائر** — يقرأ بلا تسجيل دخول.
- **الشيخ** — يقرأ جدوله عبر رابط عام يُرسَل إليه. لا حساب له ولا يحرّر شيئاً.
- **المشرف** — يُنشئ ويعدّل عبر لوحة التحكم. محرّر واحد بكلمة مرور واحدة.

---

## ٢ · المدخلات التي تجدها في المستودع

| الملف | ما هو | كيف تتعامل معه |
|---|---|---|
| `docs/schema.sql` | مخطط قاعدة البيانات كاملاً | مرجع البنية. لا تُنشئ جداول من رأسك |
| `prototype/index.html` | نموذج واجهة الزائر — **معتمد** | مرجع الشكل والسلوك. انقل منه الأنماط والبنية |
| `prototype/admin.html` | نموذج لوحة التحكم — **معتمد** | نفس الشيء |
| `docs/DECISIONS-SUMMARY.md` | حصيلة القرارات | مرجع عند أي التباس |
| `docs/adr/*.md` | أربعة قرارات معمارية | اقرأها قبل مخالفة أي قاعدة أدناه |

**النموذجان معتمدان من صاحب المشروع بعد سبع جولات تعديل.** مهمتك نقلهما إلى Next.js بأقرب ما يمكن من التطابق البصري والسلوكي، لا إعادة تصميمهما. أي انحراف عن شكلهما يحتاج إذناً صريحاً.

---

## ٣ · التقنية

- Next.js 15 · App Router · TypeScript
- Supabase (PostgreSQL + Storage)
- Tailwind CSS
- خط Tajawal
- النشر على Vercel

**ألوان الهوية — لا تُغيَّر:**
بترولي `#2A3F46` · بترولي داكن `#1C2C32` · بني رمادي `#7D7168` · رمادي دافئ `#8C847D` · بيج `#F3EFE9` · ورقي `#FCFAF7` · حدّ `#E2DCD3` · تنبيه `#C2452D` · ملغى `#6E3B33`

---

## ٤ · قبل أي كود — أربع خطوات تحقق إلزامية

لا تكتب سطر كود واحداً قبل إنجازها وعرض نتائجها.

**٤.١ · تحقّق أن قاعدة البيانات جاهزة.**
شغّل: `select count(*) from information_schema.tables where table_name in ('settings','sheikhs','series','lectures')`
إن لم يعد الرقم ٤، توقّف وأبلغ صاحب المشروع أن `schema.sql` لم يُشغَّل بعد. لا تُنشئ الجداول بنفسك.

**٤.٢ · تحقّق أن التقويم الهجري يعمل في بيئة الخادم.**
شغّل في Node بنسخته المستعملة في المشروع:
    new Intl.DateTimeFormat('ar-SA-u-ca-islamic-umalqura',{day:'numeric',month:'long',year:'numeric',timeZone:'Asia/Riyadh'}).format(new Date())
إن لم يعد تاريخاً هجرياً عربياً صحيحاً، فبيئة Node مبنية بـ ICU مصغّر. **توقّف وأبلغ** — الحل إما `full-icu` أو مكتبة تحويل، وهذا قرار يُعرض ولا يُتّخذ ذاتياً.

**٤.٣ · تحقّق أن العرض `v_lectures` يعطي الحالات.**
شغّل: `select status, count(*) from v_lectures group by 1`
يجب أن تظهر قيم من: `upcoming` · `live` · `done` · `cancelled`. إن فشل الاستعلام فالعرض لم يُنشأ.

**٤.٤ · تحقّق أن مخزن الشعار موجود.**
شغّل: `select id, public from storage.buckets where id='branding'`
يجب أن يعود صفّ واحد بـ `public = true`.

---

## ٥ · الصفحات

### واجهة الزائر

| المسار | المحتوى |
|---|---|
| `/` | شريط اللقاء القادم بعدّاد حيّ · تبويبان (الجدول · التقويم) · تصفية |
| `/sheikh/[slug]` | لقاءات شيخ واحد |
| `/s/[slug]` | صفحة السلسلة: رأس تعريفي وشريط تقدّم، ثم لقاءاتها |

**جدول الزائر — ثمانية أعمدة موسّطة كلها:**
الترتيب · اللقاء (والكتاب تحته بخط صغير) · الشيخ · الموعد · النوع · الوجهة · العدّاد · زرّ التقويم.

- **الترتيب**: رقم مجرّد يبدأ من ١ ضمن السلسلة. بلا «من ١٢». يظهر حتى في اللقاء المنفرد.
- **الموعد**: عمود واحد يجمع التاريخ الهجري واليوم والوقت والمدة.
- **الوجهة**: عمود واحد تفريعي — حضوري: المكان ورابط خرائطه · عن بُعد: «اضغط هنا للدخول» · كلاهما: الاثنان.
- **العدّاد**: يعرض الأيام فقط متى بقي أكثر من ٢٤ ساعة، ويتحوّل إلى «ساعة:دقيقة» بلون تنبيهي دون ذلك.
- **زرّ التقويم**: يولّد ملف `.ics` للقاء الواحد.

**في `/s/[slug]` يسقط عمودا «اللقاء» و«الشيخ»** لأنهما ثابتان في السلسلة.

**على شاشة أضيق من ٩٦٠ بكسل** يُستبدل الجدول ببطاقات: كل بطاقة صفٌّ واحد فيه عنوان اللقاء واسم الشيخ وزرّ «التفاصيل» والعدّاد. والنقر يفتح نافذة بكل الحقول وفيها زرّ إغلاق.

**التقويم**: شبكة شهر هجري، اليوم الذي فيه لقاء يُعلَّم، والنقر يفتح تفاصيل لقاءات ذلك اليوم. لا يعرض كل الحقول داخل الخلية.

**التصفية**: ثلاثة فقط — قادم/سابق · الشيخ · النوع. الترتيب ثابت بالأقرب زمناً. **لا فرز يدوي بالأعمدة ولا بحث نصّي.**

### لوحة التحكم

| المسار | المحتوى |
|---|---|
| `/admin/login` | حقل كلمة مرور واحد |
| `/admin` | أربعة تبويبات: اللقاءات · السلاسل · المشايخ · الإعدادات |

التبويبات شريط علوي على الشاشة الواسعة، و**شريط سفلي ثابت** دون ٨٢٠ بكسل.

**تبويب اللقاءات** — جدول بتصفية (السلسلة · قادم/سابق)، وزرّ «تعديل» لكل لقاء يفتح نافذة فيها: التاريخ، الوقت، المدة، النوع، المكان، رابط الدخول، ومفتاح «إلغاء هذا اللقاء». الحقول الفارغة ترث السلسلة، وقيمتها الموروثة تظهر كنصّ إرشادي داخل الحقل. اللقاء الذي خرج عن سلسلته يُوسم في الجدول بـ«مختلف عن السلسلة».

**تبويب السلاسل** — جدول فيه رابط كل سلسلة وزرّ نسخ، وزرّ «سلسلة جديدة» يفتح نموذجاً فيه:
العنوان · رابط الصفحة (يُقترح آلياً من العنوان ويُعدَّل) · الكتاب (اختياري) · الشيخ · النوع · المكان (مُعبّأ من الإعدادات) · رابط الدخول · تاريخ أول لقاء · الوقت · المدة · التكرار · العدد.
وتحته **معاينة حيّة للتواريخ المولّدة بالهجري**، بجانب كل موعد زرّ حذف، والترقيم يُعاد بعد الحذف.

> **مُحدَّث بعد هجرة ٠٠٢ (النموذج القالبي والأرشفة):** لكل سلسلة زرّا «أرشفة» و«حذف» إضافةً إلى «نسخ». الأرشفة تُخفيها ولقاءاتها عن الزائر وتبقى في اللوحة، قابلة للاسترجاع. والحذف نهائيّ ويأخذ لقاءاتها معه (الأثر ٨.٢)، خلف نافذة تأكيد تذكر عددها. مفتاح «إظهار المؤرشف» يكشف المؤرشفة في الجدول.

**تبويب المشايخ** — الاسم، رابط الصفحة، عدد السلاسل، مفتاح نشط/غير نشط.

> **مُحدَّث بعد هجرة ٠٠٢:** أُضيف زرّ «حذف» — نقض صريح للنصّ الأصلي أدناه، بموافقة صاحب المشروع. راجع القاعدة ٦.٦ المحدَّثة لسبب ذلك وشرحه.

**تبويب الإعدادات** — رفع الشعار (سحب وإفلات أو اختيار ملف، بمعاينة على خلفية بترولية) · اسم المقر ورابط خرائطه · المدة الافتراضية · وحقول معطّلة تعرض المنطقة الزمنية والتقويم.

---

## ٦ · قواعد لا تُخالَف

**٦.١ · الحالة مشتقّة لا مخزّنة.** لا عمود `status` في قاعدة البيانات. تُحسب من `starts_at` و`duration_min` و`is_cancelled` في العرض `v_lectures`. اقرأها من هناك ولا تعِد حسابها بمنطق موازٍ في الواجهة، وإلا اختلفت الحالة بين الجدول والتقويم.

**٦.٢ · لا تخزين مؤقّت لأي صفحة تقرأ `v_lectures`.** العرض يستعمل `now()`، فأي تخزين مؤقّت يجمّد «جارٍ الآن» والعدّاد. اضبط على هذه الصفحات:
    export const dynamic = 'force-dynamic'
    export const revalidate = 0
والعدّاد نفسه يعمل في المتصفح بمؤقّت، لا بإعادة طلب من الخادم.

**٦.٣ · الميلادي مصدر الحقيقة.** التخزين `timestamptz` والإدخال ميلادي، والهجري للعرض وحده. لا تخزّن تاريخاً هجرياً في أي عمود.

**٦.٤ · مفتاح `service_role` لا يغادر الخادم.** يُستعمل حصراً داخل `app/api/**` أو `lib/server/**`. لا يُستورد في أي ملف فيه `'use client'`، ولا يُسمّى بادئته `NEXT_PUBLIC_`.

**٦.٥ · كل كتابة تمرّ عبر الخادم.** سياسات RLS تمنح القراءة للجميع ولا تمنح كتابةً لأحد. أي محاولة كتابة من المتصفح بمفتاح `anon` ستفشل صامتةً — هذا متوقّع ومقصود. المسار الصحيح: Route Handler يفحص كوكي المشرف ثم يكتب بمفتاح الخدمة.

**٦.٦ · الشيخ يُخفى دائماً، ويُحذف من القائمة بعد هجرة ٠٠٢.** الإخفاء بـ`is_active = false` كما كان: الشيخ غير النشط يخرج من قائمة اختيار السلسلة ومن تصفية الزائر، وتبقى لقاءاته ظاهرة في «السابقة».
> **تحديث (هجرة ٠٠٢ — النموذج القالبي):** جدول `sheikhs` صار قائمة **قوالب** لا كياناً مرجعياً حيّاً. اسم الشيخ ورابطه يُنسخان لقطةً إلى `series.sheikh_name` و`series.sheikh_slug` عند إنشاء السلسلة، والقيد صار `on delete set null` بدل `restrict`. فحذف الشيخ من القائمة **لا يمسّ سلسلة ولا لقاءً ولا حتى رابطه العام** `/sheikh/<slug>` — طالما بقيت له سلسلة، لأن الهوية تُقرأ من اللقطة لا من الجدول. هذا نقضٌ صريح للنصّ الأصلي أعلاه، اتُّخذ بموافقة صاحب المشروع بعد أن اصطدم بحاجة حذف بيانات خاطئة الإدخال؛ التفصيل في `DECISIONS.md`.

**٦.٧ · اللقاء الملغى لا يُحذف.** يبقى ظاهراً للزائر مشطوباً بوسم «ملغى»، وعدّاده متوقّف.

**٦.٨ · لقاء غير حضوري بحت يلزمه رابط دخول.** القيد `series_link_required` يحرس السلسلة، لكنه **لا يحرس تجاوز اللقاء المفرد** — إن غيّر المشرف نوع لقاء إلى «عن بُعد» ولا رابط فيه ولا في سلسلته، يجب أن يرفض الخادم الحفظ برسالة واضحة. تحقّق من القيمة **الفعّالة بعد الوراثة**، لا من حقل اللقاء وحده.

---

## ٧ · حالات الفشل — مطلوب التعامل معها صراحةً

| الحالة | السلوك المطلوب |
|---|---|
| قاعدة البيانات لا تستجيب | صفحة خطأ عربية مفهومة، لا شاشة بيضاء ولا نصّ إنجليزي |
| لا لقاءات قادمة | الشريط العلوي يعرض «لا لقاءات قادمة حالياً»، والجدول يعرض حالة فارغة مفيدة |
| التصفية لا تطابق شيئاً | «لا لقاءات تطابق هذه التصفية» مع اقتراح إزالتها |
| `slug` غير موجود | صفحة ٤٠٤ عربية فيها رابط للصفحة الرئيسية |
| كلمة مرور خاطئة | رسالة عربية، وتأخير تصاعدي بعد خمس محاولات فاشلة من العنوان نفسه |
| رفع ملف غير صورة أو يتجاوز ٢ ميجابايت | رفض برسالة عربية قبل الرفع |
| فشل توليد التواريخ (تاريخ غير صالح) | منع الحفظ برسالة، لا إنشاء سلسلة بلا لقاءات |

---

## ٨ · آثار جانبية معروفة — لا تعالجها بنفسك

هذه نتائج مقصودة لقرارات موثّقة. **اذكرها في تقريرك النهائي ولا تغيّر التصميم لتفاديها.**

**٨.١** تغيير «اسم المقر» في الإعدادات يغيّر المكان المعروض في **كل** اللقاءات التي لم يُكتب لها مكان خاص — بما فيها اللقاءات الماضية. هذا أثر مباشر للوراثة بالمرجع لا بالنسخ. اعرضه على صاحب المشروع في تقريرك، ولا تُنشئ نسخاً تاريخية من تلقاء نفسك.

**٨.٢** حذف سلسلة يحذف لقاءاتها كلها (`on delete cascade`). لوحة التحكم يجب أن **تطلب تأكيداً صريحاً يذكر عدد اللقاءات** التي ستُحذف.

**٨.٣** لا سجلّ تعديلات ولا معرفة بمن عدّل — أثر مباشر لاختيار كلمة المرور الواحدة (ADR-0004). ولذلك **لا تراجُع بعد حذف نهائي**: نافذتا التأكيد في اللوحة هما فرصة المراجعة الوحيدة قبل الحذف، ويحرسهما الخادم بمطابقة عدد (`?expect=`) بين لحظة العرض ولحظة الضغط.

**٨.٤** (هجرة ٠٠٢) تعديل اسم شيخ في قائمة القوالب **لا يسري رجعياً**: السلاسل القائمة تحمل لقطة الاسم وقت إنشائها، فتبقى بالاسم القديم حتى لو عُدِّل القالب لاحقاً. هذا أثر مقصود للنموذج القالبي — يعرضه صاحب المشروع في تقريره ولا يُصلَح بمزامنة تلقائية.

---

## ٩ · معايير القبول — كلها تُثبَت بمخرج، لا بالنظر

**لا يُطلب منك إثبات بصريّ.** الحكم على الشكل لصاحب المشروع، وحدّك أن تطابق النموذجين. أما هذه فأثبتها بمخرج ملموس:

١. `select count(*) from v_lectures where status='live'` — يعمل بلا خطأ.
٢. طلب `/` يعيد ٢٠٠، ونصّه يحوي «اللقاء القادم» أو «لا لقاءات قادمة حالياً».
٣. ترويسة استجابة `/` تحوي `cache-control` بقيمة تمنع التخزين.
٤. طلب `/admin` بلا كوكي يعيد تحويلاً إلى `/admin/login`.
٥. طلب `/admin` بكوكي صالح يعيد ٢٠٠.
٦. البحث النصّي في المستودع عن `SUPABASE_SERVICE_ROLE_KEY` لا يعطي أي نتيجة في ملف فيه `'use client'`.
٧. البحث النصّي عن `NEXT_PUBLIC_SUPABASE_SERVICE` لا يعطي أي نتيجة إطلاقاً.
٨. محاولة كتابة في `lectures` بمفتاح `anon` تُرفض — أرفق نصّ الرفض.
٩. `/s/<slug-موجود>` يعيد ٢٠٠ ونصّه يحوي «أُنجز».
١٠. `/sheikh/<slug-غير-موجود>` يعيد ٤٠٤.
١١. إنشاء سلسلة بأربعة لقاءات ثم حذف موعد من المعاينة يُنتج **ثلاثة** صفوف ترتيبها ١ و٢ و٣.
١٢. تعديل لقاء إلى «عن بُعد» بلا رابط يُرفض — أرفق نصّ الرسالة.

**لا تستعمل عدّ الأسطر دليلاً على أي شيء.** يُخدع بحذف وإضافة متساويين. أثبت بالبحث النصّي عن المحتوى.

---

## ١٠ · ما لا يُبنى

التسجيل في اللقاءات · كشوف الحضور · الشهادات · روابط تسجيلات مرئية بعد اللقاء · تنبيهات آلية بواتساب أو بريد · اشتراكات · بحث نصّي · فرز يدوي بالأعمدة · تصفية بالكتاب · حسابات للمشايخ · حسابات مشرفين متعددة · سجلّ تعديلات · إحصاءات وعدّاد زيارات · تعدّد لغات.

ولا ربط بأي قاعدة بيانات أخرى — المشروع مستقل تماماً عن منصة الطلاب (ADR-0003).

---

## ١١ · توقّفات إجبارية

قف واسأل صاحب المشروع، ولا تجتهد:

- فشل أي خطوة من الأربع في القسم ٤.
- الحاجة إلى تعديل `schema.sql`: إضافة عمود أو جدول أو تغيير قيد.
- أي أمر SQL يحذف أو يعدّل بيانات قائمة.
- أي انحراف عن شكل النموذجين المعتمدين.
- أي بند في القسم ١٠ بدا لك لازماً.
- تعارض بين هذه الوثيقة وملف من ملفات `docs/`.

---

## ١٢ · ترتيب البناء

١. تهيئة المشروع والاتصال بـ Supabase، وتنفيذ فحوص القسم ٤ وعرض نتائجها.
٢. طبقة القراءة: أنواع TypeScript ودوالّ جلب من `v_lectures`.
٣. `/` بالجدول والبطل والتصفية والحالات الفارغة.
٤. البطاقات ونافذة التفاصيل على الجوال.
٥. تبويب التقويم.
٦. `/sheikh/[slug]` و`/s/[slug]`.
٧. المصادقة: `/admin/login` والكوكي والحراسة.
٨. تبويبا اللقاءات والسلاسل مع توليد التواريخ.
٩. تبويبا المشايخ والإعدادات مع رفع الشعار.
١٠. توليد ملف `.ics`.
١١. جولة معايير القبول الاثني عشر، وتقرير نهائي يذكر الآثار الجانبية في القسم ٨.

**بعد كل مرحلة: تقرير قصير بما أُنجز وما بقي، ثم انتظر.** لا تبنِ المشروع كاملاً دفعة واحدة.
