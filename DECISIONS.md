# DECISIONS — ذاكرة المشروع

منصة اللقاءات العلمية · جمعية سنن التعليمية
مرجع البناء: `BUILD-INSTRUCTION.md` · القرارات السابقة: `docs/DECISIONS-SUMMARY.md` و `docs/adr/`

هذا الملف يسجّل ما استُجدّ **أثناء البناء**. ما قبله موثّق في `docs/`.

---

## القرارات

### 2026-08-31 إسقاط وسم «أُجّل»
- **ماذا:** لا وسم «أُجّل» في المنصة. التأجيل تعديلُ تاريخٍ فحسب، بلا أثر مرئي أو مخزّن.
- **لماذا:** `docs/DECISIONS-SUMMARY.md:45` يطلبه، ولا مكان له في أي مخرج معتمد: لا ذِكر له في `BUILD-INSTRUCTION.md`، ولا عمود في `docs/schema.sql`، ولا مفتاح في `prototype/admin.html`، ولا وسم في `prototype/index.html`. والقاعدة ٦.١ تحصر اشتقاق الحالة في `starts_at` و`duration_min` و`is_cancelled`.
- **البديل المرفوض:** تنفيذه — يلزمه عمود `postponed_from` في `lectures` (تعديل على المخطط) وحقل في نافذة التعديل ووسم في جدول الزائر (انحراف عن نموذجين معتمدين بعد سبع جولات).
- **الافتراضات:** أن التأجيل نادر، وأن الزائر يكفيه رؤية التاريخ الجديد. لو تكرّر التأجيل وصار الشيخ يسأل «أين الموعد القديم؟» فسيلزم إعادة فتح القرار — وحينها يُضاف العمود بلا هدم بيانات.
- **يَنسخ:** السطر ٤٥ من `docs/DECISIONS-SUMMARY.md`.

### 2026-08-31 نقل أنماط النموذجين كـ CSS حرفياً لا إعادة صياغتها بـ Tailwind
- **ماذا:** أنماط `prototype/index.html` و`admin.html` تُنقل كـ CSS كما هي. Tailwind يبقى في الحزمة ويُعرِّف ألوان الهوية كأدوات (`@theme`)، ولا يُعاد بناء التخطيط به.
- **لماذا:** التعليمة تطلب «أقرب ما يمكن من التطابق البصري»، والنموذجان معتمدان بعد سبع جولات تعديل. إعادة صياغة ٢٥٠ سطر CSS مضبوطة إلى أصناف Tailwind تُنتج انحرافاً بصرياً مضموناً بلا مقابل.
- **البديل المرفوض:** إعادة الكتابة بأصناف Tailwind — أنظف نظرياً، لكن الحكم على الشكل لصاحب المشروع والمطلوب المطابقة لا الأناقة.
- **الافتراضات:** أن النموذجين لن يُعاد تصميمهما. لو تغيّر التصميم جذرياً لاحقاً فقد يستحق الانتقال إلى Tailwind كاملاً.

### 2026-08-31 بيانات تجريبية في ملف مستقل بتواريخ نسبية
- **ماذا:** `docs/seed.sql` منفصل عن `schema.sql`، وكل تواريخه محسوبة من `now()` بإزاحة بالأيام، وفي آخره أمر حذف واحد.
- **لماذا:** أربعة من معايير القبول الاثني عشر لا تُثبَت على قاعدة فارغة (المعايير ١ و٩ و١١ و١٢)، وفحص ٤.٣ نفسه يطلب ظهور الحالات الأربع. وتواريخ ثابتة تصير كلها ماضية خلال أسبوع فينطفئ العدّاد ووسم «جارٍ الآن».
- **البديل المرفوض:** دمجها في `schema.sql` — يخلط البنية بالبيانات ويجعل الحذف قبل النشر محفوفاً بالخطأ.
- **الافتراضات:** أن اللقاء الجارٍ يبقى `live` نحو ساعتين و٤٥ دقيقة بعد الزرع فقط. تجديده بحذف وإعادة زرع.

### 2026-08-31 مفاتيح Supabase الحديثة بدل مفاتيح JWT القديمة
- **ماذا:** المشروع يستعمل `sb_publishable_…` و`sb_secret_…` لا `eyJ…`.
- **لماذا:** مفاتيح JWT القديمة معطّلة في المشروع الجديد. ثبت بالفحص: المفتاحان يفكّان صحيحين (`ref` و`role` و`exp` كلها سليمة) ومع ذلك يردّ الخادم `401 Invalid API key`.
- **البديل المرفوض:** تفعيل المفاتيح القديمة من اللوحة — يعمل، لكنه تمسّك بآلية تتّجه Supabase إلى إلغائها.
- **الافتراضات:** أن `@supabase/supabase-js` (2.112+) يقبل الصيغة الجديدة في موضع المفتاح نفسه — وهو كذلك. والمفاتيح نفسها تُوضع في متغيّرات بيئة Vercel عند النشر.

### 2026-08-31 استعلام إضافي بدل تعديل العرض `v_lectures`
- **ماذا:** العرض لا يحمل `series.slug`، وصفّ الجدول يحتاجه ليربط عنوان اللقاء بصفحة سلسلته. نجلب خريطة `series_id ← slug` باستعلام واحد إضافي للصفحة كلها (`getSeriesSlugMap`).
- **لماذا:** إضافة عمود إلى العرض تعديلٌ على `docs/schema.sql` — توقّف إجباري بالقسم ١١. والاستعلام الإضافي رخيص: صفّ لكل سلسلة، خمسة صفوف اليوم.
- **البديل المرفوض:** تعديل العرض ليحمل `s.slug as series_slug` — أنظف تقنياً، لكنه يستدعي إذناً ويكسر قاعدة «لا تُنشئ جداول ولا تعدّل المخطط من رأسك».
- **الافتراضات:** أن عدد السلاسل يبقى بالعشرات لا بالآلاف. لو تضخّم فالأنسب طلب تعديل العرض بإذن صريح.

### 2026-08-31 وعاء «قادم/سابق» يُحسب في الواجهة لا في العرض
- **ماذا:** الحالة تأتي كاملةً من `v_lectures` ولا تُحسب في الواجهة (القاعدة ٦.١). الشيء الوحيد الذي يُقرَّر محلياً هو **الوعاء**: هل يظهر اللقاء تحت «القادمة» أم «السابقة».
- **لماذا:** اللقاء الملغى حالته `cancelled` سواء كان موعده غداً أو قبل شهر، فالحالة وحدها لا تفصل بينهما. النموذج المعتمد يضع الملغى القادم في «القادمة» مشطوباً — فنرجع إلى `starts_at` لتحديد الوعاء وحده.
- **البديل المرفوض:** إضافة عمود `is_future` إلى العرض — تعديل مخطط لأجل تصنيف عرضي.
- **الافتراضات:** أن الفارق الزمني بين ساعة الخادم وساعة قاعدة البيانات مللي ثوانٍ. لو تباعدا فقد يقع لقاء ملغى على حدّ اللحظة في الوعاء الخطأ لثوانٍ — أثر مقبول لأنه لا يمسّ حالة ولا عدّاداً.

### 2026-08-31 تحديث الحالة بالتسليح على الحدّ بدل نبضة الثلاثين ثانية
- **ماذا:** العدّاد يدقّ محلّياً كل دقيقة. وتغيّر الحالة يأتي من الخادم: مؤقّت واحد مسلَّح على أقرب لحظة تغيّر (بداية قادم أو نهاية جارٍ) يستدعي `router.refresh()`، ومعه تحديث عند العودة إلى التبويب وشبكة احتياطية كل دقيقة والصفحة ظاهرة.
- **لماذا:** النموذج يعيد حساب الحالة في المتصفح كل ٣٠ ثانية، والقاعدة ٦.١ تمنع ذلك. ولو اكتفينا بالعدّاد المحلّي بلا تحديث، لبقي لقاءٌ بدأ معروضاً «قادم» وعدّاده على ٠٠:٠٠ حتى يُعيد الزائر التحميل — سلوك أسوأ من النموذج.
- **البديل المرفوض:** ترقية الحالة في المتصفح عند بلوغ الصفر — أبسط، لكنه نقض حرفيّ للقاعدة ٦.١ ويُنتج اختلافاً بين الجدول والتقويم.
- **الافتراضات:** أن انحراف ساعة الزائر عن ساعة الخادم ثوانٍ لا دقائق. أُضيف هامش ثانيتين بعد الحدّ. ولا يعمل شيء والصفحة مخفيّة، فلا تُستنزف قاعدة البيانات بتبويبات منسيّة.

### 2026-08-31 الخادم يُنسّق التواريخ والعميل يعرضها
- **ماذا:** كل نصّ تاريخ أو وقت يُبنى في الخادم ويصل العميل جاهزاً (`hijri`, `weekdayName`, `time`)، ومعه لحظات مطلقة أرقاماً (`startsAtMs`). مكوّنات العميل لا تستدعي `Intl` إلا في تنقّل أشهر التقويم.
- **لماذا:** `Intl` ليست نقيّة عبر البيئات: اختلاف إصدار ICU أو نظام الأرقام المحلول بين Node والمتصفح يُنتج نصّاً مختلفاً في العقدة نفسها، فيقع خطأ ترطيب على كل صفّ. وتمرير الأرقام بدل السلاسل يلغي تحليل التواريخ من طرف المتصفح أصلاً.
- **البديل المرفوض:** تنسيق في الطرفين كما يفعل النموذج — النموذج ملف ساكن بلا خادم فلا مقارنة ترطيب فيه.
- **الافتراضات:** أن شهر البداية في التقويم يُبنى خادمياً ويُستعمل كما هو حتى ينتقل الزائر — فلا Intl في المتصفح لأول تصيير.

### 2026-08-31 التصفية في معاملات الرابط
- **ماذا:** `?period=&sheikh=&type=` بقيم قاعدة البيانات وslug الشيخ. القيم الافتراضية تُحذف فيبقى `/` هو الشكل القانوني. أما التبويب وشهر التقويم فحالة عميل.
- **لماذا:** الرابط يصير قابلاً للمشاركة وزرّ الرجوع يعمل. والأهم أن الوعاء «قادم/سابق» يعتمد على `now()` وعلى الحالة، فترشيحه في الخادم يعيد الحساب عند كل تغيير؛ ولو رُشِّح في المتصفح على حمولة قديمة لاختلف تصنيف اللقاء بين الجدول والتقويم.
- **البديل المرفوض:** حالة عميل كما في النموذج — تُبقي الترشيح على بيانات قد تتقادم.
- **الافتراضات:** أن كلفة جولة قاعدة بيانات عند كل تغيير مقبولة — وهي كذلك، فالصفحة بلا تخزين مؤقّت أصلاً (٦.٢) ولا ذاكرة وسيطة تُبطَل.

### 2026-08-31 ملف `.ics` من معالج مسار لا من المتصفح
- **ماذا:** `/api/ics/<id>` يعيد `text/calendar` بترويسة `Content-Disposition: attachment`. والزرّ بقي `<button className="ics">` كما في النموذج وينقل إليه.
- **لماذا:** النموذج يبني الملف بـ`URL.createObjectURL` وينقر وسماً غير مُلحَق بالمستند. Safari على iOS لا يحترم سمة `download` لرابط blob، فلا يحدث شيء — وهذا الزرّ هو البديل الوحيد للتنبيهات الآلية بحسب حصيلة القرارات §٦.
- **البديل المرفوض:** تحويل الزرّ إلى `<a download>` — أنظف، لكن أصناف الورقة (عرض وارتفاع ٣٤ بكسل) تفترض عنصراً كتليّاً ولا تنطبق على `<a>` سطريّ، فيتغيّر الشكل.
- **الافتراضات:** أن المسار عام بلا مصادقة — وهو كذلك: يقرأ بمفتاح `anon` من `v_lectures`، والمحتوى معلَن أصلاً.

### 2026-08-31 التقويم في الصفحتين الفرعيتين مقيَّد بنطاق الصفحة
- **ماذا:** التبويبان (الجدول · التقويم) يبقيان في `/sheikh/[slug]` و`/s/[slug]` كما في النموذج، لكن التقويم يعرض لقاءات نطاق الصفحة وحدها.
- **لماذا:** `calendar()` في النموذج يقرأ القائمة الكاملة `LX` لا المصفّاة، فتقويم صفحة شيخٍ كان سيعرض لقاءات غيره — وهو ما لا معنى له في صفحة مخصّصة تُرسَل إلى صاحبها. والقيد لا أثر له على `/` لأن نطاقها الكل أصلاً.
- **البديل المرفوض:** إسقاط تبويب التقويم من الصفحتين — القسم ٥ يذكر التبويبين تحت `/` وحدها، لكن حذف عنصر من شريط معتمد انحراف مرئي أثقل من تقييد محتواه.
- **الافتراضات:** أن تقييد المحتوى ليس انحرافاً بصرياً — الشكل والسلوك كما هما، والمعروض هو ما تدور حوله الصفحة.

### 2026-08-31 لا رأس مُخترَع لصفحة الشيخ
- **ماذا:** `/sheikh/[slug]` ليس فيها رأس تعريفي. الترويسة ثم البطل مقيَّداً بالشيخ، ثم شريط الرابط العام `#sbar` باسمه ورابطه، ثم الجدول بأعمدته الثمانية.
- **لماذا:** النموذج المعتمد لا يحمل تصميماً لرأس صفحة الشيخ — يحاكيها بالتصفية و`#sbar` وحده. وابتكار رأس على نمط `serhead` إنشاءُ عنصر لا أصل له في نموذج معتمد بعد سبع جولات، وهو توقّف إجباري بالقسم ١١.
- **البديل المرفوض:** رأس مبسّط باسم الشيخ وعدد سلاسله — أنفع للزائر، لكنه اجتهاد في موضع نُهيَ فيه عن الاجتهاد.
- **الافتراضات:** أن `#sbar` يؤدّي غرض التعريف — فهو يحمل اسم الشيخ ورابط صفحته. إن رآه صاحب المشروع ناقصاً فالقرار له.

### 2026-08-31 البطل مقيَّد بنطاق الصفحة في الصفحتين الفرعيتين
- **ماذا:** شريط البطل في `/sheikh/[slug]` و`/s/[slug]` يعرض أقرب لقاء **من لقاءات تلك الصفحة**، لا أقرب لقاء في المنصة كلها.
- **لماذا:** النموذج يبني البطل من القائمة الكاملة دائماً، فصفحة شيخٍ كانت ستُصدَّر بلقاء شيخ آخر. والقسم ٥ لا يذكر البطل إلا تحت `/`.
- **البديل المرفوض:** إسقاط البطل من الصفحتين — حذف عنصر معتمد.
- **الافتراضات:** أن الشيخ الذي يفتح رابطه يتوقّع أن يرى لقاءه القادم لا لقاء غيره.

### 2026-08-31 شطب عمود «الموعد» للملغى في صفحة السلسلة — بإذن صريح
- **ماذا:** في `/s/[slug]` وحدها، يُشطب التاريخ الهجري في صفّ اللقاء الملغى بالسماكة نفسها (1.5px).
- **لماذا:** القاعدة ٦.٧ توجب بقاء الملغى «مشطوباً». والشطب في النموذج معلّق بعمود «اللقاء» — وهو العمود الذي يسقط في صفحة السلسلة، فكان الملغى يبقى هناك بوسمه وبلا شطب.
- **البديل المرفوض:** ترك الحال — يخالف قاعدة صريحة في القسم ٦.
- **الافتراضات:** القاعدة مقيَّدة بـ`.tablecard.noseries` فلا تسري على `/` حيث الشطب واقع على العنوان أصلاً — ولا يجتمع خطّان في صفّ واحد. وهي **الإضافة الوحيدة** إلى `visitor.css` بعد النقل الحرفي، ومفصولة في آخر الملف بعنوان صريح.
- **الإذن:** صاحب المشروع، ٢٠٢٦-٠٨-٣١.

### 2026-08-31 كلمة مرور اللوحة مولَّدة لا مختارة
- **ماذا:** `ADMIN_PASSWORD` ولّدتُها بـ`crypto.randomInt` — ١٦ محرفاً من حروف وأرقام إنجليزية، بلا المحارف الملتبسة (I · O · l · o · 0 · 1).
- **لماذا:** طلب صاحب المشروع توليدها. والتوليد العشوائي أقوى من كلمة يختارها الإنسان.
- **الافتراضات:** أنها تُنسخ إلى متغيّرات بيئة Vercel عند النشر. وتغييرها يُبطل كل جلسات اللوحة القائمة (ADR-0004).

### 2026-08-31 مصادقة يدوية بإذن معماري — لا Supabase Auth
- **ماذا:** كلمة مرور واحدة في `ADMIN_PASSWORD`، تُقارَن في الخادم بزمن ثابت، ويُصدَر كوكي `httpOnly` موقّع بـHMAC-SHA256 صالح ثلاثين يوماً. مفتاح التوقيع **مشتقّ من كلمة المرور نفسها**.
- **لماذا:** ADR-0004 يرفض Supabase Auth: المحرّر واحد والمحتوى معلَن. واشتقاق مفتاح التوقيع من الكلمة يجعل تغييرها في Vercel يُبطل كل الجلسات فوراً — وهو ما يوجبه ADR-0004 لإبطال الوصول عند تسرّبها.
- **البديل المرفوض:** Supabase Auth — وهو ما توصي به مهارة الفحص الأمني افتراضياً. رُفض بقرار سابق للبناء، لا اجتهاداً؛ والمسؤولية التي كانت الخدمة تتكفّل بها تولّاها `lib/server/auth.ts` صراحةً: مقارنة بزمن ثابت على بصمة موحّدة الطول، وكوكي لا يحمل الكلمة ولا يشتقّ منها شيء يُقرأ.
- **الافتراضات:** أن المحرّر يبقى واحداً. والانتقال إلى حسابات متعددة لاحقاً لا يهدم بيانات (ADR-0004).

### 2026-08-31 الحجب التصاعدي في ذاكرة العملية
- **ماذا:** بعد خمس محاولات فاشلة من العنوان نفسه، حجبٌ يتضاعف: ٥ ثوانٍ ثم ١٠ ثم ٢٠ بسقف خمس دقائق. والردّ فوري برمز ٤٢٩ ومدّة الانتظار بالعربية.
- **لماذا:** القسم ٧ يوجب «تأخيراً تصاعدياً». وإبقاء الاتصال مفتوحاً عقوبةً يستهلك دالّة الخادم فيصير هو نفسه باباً لإرهاق الخدمة — فالحجب أسلم من التأخير.
- **البديل المرفوض:** جدول محاولات في قاعدة البيانات — أدقّ عبر النسخ، لكنه تعديل على المخطط (توقّف إجباري بالقسم ١١).
- **الافتراضات:** أن العدّ يكفي للحماية من التخمين اليدوي. على Vercel قد تتعدّد نسخ الدالّة فيبدأ العدّ من جديد في نسخة أخرى — مذكور في الفجوات.

### 2026-09-01 كل كتابة عبر معالج مسار يفحص الكوكي بنفسه
- **ماذا:** `PATCH /api/admin/lectures/[id]` و`POST /api/admin/series` — كلاهما يستدعي `requireAdmin()` قبل أن يلمس مفتاح الخدمة، ولا يعتمد على حراسة التخطيط.
- **لماذا:** حراسة التخطيط تحمي **العرض** وحده. من يعرف عنوان المعالج ينادِيه مباشرةً بلا أن يمرّ بصفحة، فحارس العرض لا يراه أصلاً. القاعدة ٦.٥ تشترط أن تمرّ الكتابة بفحصٍ لا بصفحة.
- **البديل المرفوض:** الاكتفاء بحارس التخطيط — مُثبَت خطؤه: `PATCH` و`POST` بلا كوكي يعيدان `401` بفضل الحارس في المعالج لا في التخطيط.
- **الافتراضات:** أن كل معالج يُضاف لاحقاً يستدعي `requireAdmin()` أولاً. لا يفرضه المترجم — يفرضه الانتباه.

### 2026-09-01 التحقّق من المدخلات في الخادم قبل قيود قاعدة البيانات
- **ماذا:** `lib/server/validate.ts` يفحص كل حقل قبل الكتابة: صيغة التاريخ والوقت، والمدة بين ٥ و٦٠٠، والنوع من القيم الثلاث، والرابط `https`/`http` وحدهما، والـslug بصيغته.
- **لماذا:** قيود المخطط حارسٌ أخير لا أول، ورسالتها إنجليزية غامضة (`violates check constraint`) والقسم ٧ يوجب رسالة عربية مفهومة. ومنعُ `javascript:` في الروابط ليس تجميلاً: القيمة تُعرض للزائر داخل `href`.
- **البديل المرفوض:** الاعتماد على قيود قاعدة البيانات وحدها — تحمي البيانات ولا تحمي المستخدم من رسالة لا يفهمها.
- **الافتراضات:** أن قيود المخطط تبقى الحدّ الأخير: كل رسالة قيد تُترجَم عربياً في المعالج، فلو ظهر قيد جديد لظهرت رسالته العامة لا الإنجليزية.

### 2026-09-01 لا زرّ حذف للسلسلة في اللوحة
- **ماذا:** تبويب السلاسل يعرض الرابط وزرّ نسخ فحسب، بلا حذف.
- **لماذا:** القسم ٥ لا يذكر حذفاً، والنموذج المعتمد `vSer()` لا يحويه. وإضافته إنشاءُ عنصرٍ لا أصل له.
- **أثره على ٨.٢:** الأثر الجانبي ٨.٢ يوجب — إن وُجد الحذف — تأكيداً صريحاً يذكر عدد اللقاءات. وهو **لا يقع ما دام الحذف غير معروض**. فإن أراد صاحب المشروع الحذف لاحقاً، فالتأكيد شرطٌ لازم لبنائه.
- **الافتراضات:** أن السلسلة الخاطئة تُصحَّح لا تُحذف. ولو لزم الحذف فمن SQL بيد صاحب المشروع حتى يُبنى بإذن.

### 2026-09-01 رفض SVG للشعار — أمانٌ يخالف حرف النموذج
- **ماذا:** يقبل رفع الشعار PNG و JPEG و WebP وحدها. SVG مرفوض في المتصفح وفي الخادم معاً، والنوع يُستنتج من **بايتات الملف** لا من ترويسة المتصفح.
- **لماذا:** ثلاث طبقات متراكبة — (١) SVG وحده بلا رقم سحري، فلا سبيل للتأكّد أنه SVG لا HTML؛ (٢) مخزن `branding` عام ورابطه يُخدَم `inline`، ففتحه في تبويب يجعله **وثيقة عليا** ينفَّذ فيها `<script>`، لا صورة؛ (٣) Supabase يعيد بثّ الـ`content-type` المخزَّن، فلو مُرِّرت قيمة العميل كما جاءت لأمكن تخزين ملف بـ`text/html` على مخزن عام. مُثبَت بالتجربة: نصّ باسم `.png` وSVG فيه سكربت — كلاهما مرفوض.
- **البديل المرفوض:** تنقية SVG بقائمة سوداء — يُتحايَل عليها بالكيانات وCDATA وفضاءات الأسماء. والتخفيف الوحيد المعتدّ به تحويل المخزن إلى خاص وخدمته عبر معالجنا بترويسة `CSP: sandbox`، وذلك تعديل على المخطط ⇐ توقّف إجباري.
- **الأثر على النموذج:** قائمة `accept` في النموذج تحوي `image/svg+xml`، ونصّ التلميح يقول «PNG أو SVG». صار «PNG أو WebP». **انحراف عن نموذج معتمد يحتاج إذناً (§١١) — يُعرض على صاحب المشروع.**
- **تخفيف الشدّة بأمانة:** الرفع محصور بالمشرف، فليس باباً مفتوحاً للعالم. الخطر الواقعي أن يُخدَع المشرف بملف يُرسَل إليه على أنه شعار.

### 2026-09-01 اسم فريد لكل شعار وحذف القديم
- **ماذا:** المفتاح `logo-<uuid>.<ext>` يُولَّد في الخادم، واسم الملف من المستخدم لا يدخل المسار. وبعد كل رفع ناجح يُحذف الكائن القديم؛ و«الإزالة» تحذف الملف **ثم** تُفرّغ العمود.
- **لماذا:** ثلاث مشكلات يحلّها الاسم الفريد دفعةً: `upsert` افتراضه `false` فالمسار الثابت يفشل في الرفع الثاني؛ وكائنات Supabase تُخدَم بـ`cache-control: 3600` فالمسار الثابت يُبقي القديم في الحواف؛ واسم المستخدم قد يحمل `../` أو محارف تحكّم تُخفي الامتداد. وحذف القديم يمنع تراكماً لا حدّ له في مخزن عام.
- **البديل المرفوض:** `upsert:true` على `logo.png` ثم `?v=` في الرابط — يعالج المتصفح ولا يعالج الحواف، ويجعل العمود يحمل استعلاماً يكسر مقارنة المفتاح عند الحذف.
- **مُثبَت:** بعد رفعين يبقى في المخزن **ملف واحد**؛ وبعد «الإزالة» يبقى **صفر** ولا ملف يتيم.

### 2026-09-01 «المدة الافتراضية» حقل معطّل لا محذوف
- **ماذا:** الحقل موجود في تبويب الإعدادات بقيمته الفعلية ٩٠، لكنه `disabled` مع نصّ يبيّن أن لا عمود له في المخطط.
- **لماذا:** لا عمود لها في `settings`. والخياران كلاهما توقّف إجباري: إضافة عمود تعديلُ مخطط، وحذف الحقل انحرافٌ عن النموذج. فاخترتُ ثالثاً **قابلاً للتراجع في دقيقة**: العنصر باقٍ بشكله، ولا يكذب على المشرف بحفظٍ لا يقع.
- **البديل المرفوض:** حقل قابل للكتابة يحفظ في العدم — يرى المشرف «حُفظت الإعدادات» ولا شيء يُحفظ.
- **ينتظر:** قرار صاحب المشروع. متى أذِن بالعمود صار الحقل عاملاً بتعديل سطرين.

---

## الانحرافات

### 2026-08-31 ثلاثة فروق عن النموذج في صفحة `/` — تحتاج نظرة صاحب المشروع
- **ماذا:**
  ١. **إحياء الشطب:** الورقة فيها `tbody tr.off .title b{text-decoration:line-through}` وهي قاعدة ميتة في النموذج لأنه يضع `<button class="serlink">` مكان الـ`<b>`. فاللقاء الملغى لا يُشطب في الجدول رغم أن القاعدة ٦.٧ تطلبه. عُشِّش `<b>` داخل الرابط فأُحييت، بلا تعديل حرف في الورقة.
  ٢. **حالتان فارغتان جديدتان:** النموذج يملك رسالة واحدة تقترح «إزالة تصفية النوع»، ولو عُرضت حين لا تصفية أصلاً لاقترحت على الزائر إزالة شيء لم يضعه. أُضيفت رسالتان بالبنية والصنف `.empty` نفسيهما بلا عنصر جديد: «لا لقاءات قادمة حالياً» و«لا لقاءات سابقة».
  ٣. **زرّ «عودة لكل اللقاءات»** في شريط `#sbar` صار ينتقل إلى `/` بدل إعادة ضبط حالة عميل. وهو `<button>` كما في النموذج، فيلتقط `.sheikhbar button` ويبقى شكله مطابقاً — وكنتُ حوّلته إلى `<Link className="backlink">` فصار كبسولة، وردّته المراجعة.
- **السبب:** الأول تطبيق للقاعدة ٦.٧، والثاني للقسم ٧ الذي يفرّق بين حالتَي الفراغ، والثالث نتيجة صيرورة المسارات حقيقية.
- **الحالة:** قيد المراجعة — كلها فروق مرئية أو شبه مرئية عن نموذج معتمد (§١١).

### 2026-08-31 مراجعة عدائية على المراحل ١–٦ — ما أُصلح
- **ماذا:** مراجعة بسبعة محاور يتبع كلَّ محورٍ وكيلٌ مهمّته نقض ملاحظاته. صمدت ١٥ ملاحظة ورُدّت ٨ (منها كل ملاحظات الأمان). أُصلح منها اثنتا عشرة، أهمّها:
  · **النافذة كانت تحفظ كائن اللقاء** فتتجمّد حالتها بعد التحديث بينما ينقلب الصفّ خلفها — مخالفة ٦.١ تقع على الجوال حصراً، إذ النافذة هناك العرض الوحيد للتفاصيل. صارت تحفظ مُعرِّفاً وتشتقّ المعروض عند كل تصيير.
  · **«اللقاء القادم» في رأس السلسلة** كان يُختار بترتيب `ord` لا بالأقرب زمناً؛ ولقاءٌ أُجِّل (وقد أُسقط وسم «أُجّل» فصار التأجيل تعديلَ تاريخ) كان يعرض عدّاداً لموعد بعيد.
  · **معامل `?sheikh=`** كان يُطبَّق بلا تحقّق، فتُفرض تصفية خفيّة لا تُظهرها القائمة ولا سبيل إلى إزالتها.
  · **`setTimeout` يتجاوز سقف ٣٢ بت** حين يكون أقرب لقاء بعد أكثر من ٢٤٫٨ يوماً، فينقلب سالباً ويُقصّ إلى صفر: تحديث فوري بلا سبب ثم سقوط آلية التسليح كلها.
  · **زرّ 🗓** كان يستبدل المستند بـ`location.href`، فتعثّرُ قاعدة البيانات لحظتها يُخرج الزائر إلى صفحة بيضاء بلا رابط عودة.
  · **زرّ العودة في `#sbar`** صار كبسولة `.backlink` بدل مستطيل `.sheikhbar button` — أُعيد `<button>` كما في النموذج.
  · **قائمة تصفية المشايخ** كانت تسقط من الصفحات الفرعية، والقاعدة `#fSheikh,#fType{flex:1 1 calc(50% - 4px)}` تفترض عنصرين فينكسر الشريط على الجوال. عادت، ودورها فيها التنقّل إلى صفحة الشيخ.
  · **`preflight` من Tailwind** كان يورِّث `line-height:1.6` لكل زرّ فيتضخّم: `.tab` من ٣٧٫٠٠ إلى ٣٩٫١٩ بكسل والشريط اللاصق من ٢٠٣ إلى ٢٠٦٫٧٨. أُعيد `normal`.
  · **إمكانية الوصول:** اسم متاح للنافذة، وربط التبويبين بقسميهما، وتنقّل بالأسهم معكوس الاتجاه للـRTL.
- **السبب:** القسم ٩ يوجب إثبات كل شيء بمخرج ملموس، والمراجعة الذاتية لا تكشف ما يعميك عنه أنك كاتبه.
- **الحالة:** مقبول — كلها إصلاحات تعيد المطابقة أو تصحّح خطأ، لا تغييرات تصميم.

### 2026-08-31 أدوات تحقّق آلية غير مطلوبة في التعليمة
- **ماذا:** `scripts/check-db.mjs` و`scripts/check-secrets.mjs` و`docs/verify.sql` — ثلاث أدوات لا تذكرها التعليمة.
- **السبب:** القسم ٩ يوجب إثبات كل معيار **بمخرج ملموس** وينهى عن الاستدلال بعدّ الأسطر. الفحص اليدوي في كل جولة مُكلف وعرضة للسهو، فأُتمتته.
- **الحالة:** مقبول — لا يمسّ أي سلوك للمنصة، وأثبت معياري القبول ٦ و٧ مبكراً.

---

## الفجوات

### 2026-09-01 «المدة الافتراضية» في الإعدادات بلا عمود في المخطط
- **ماذا:** القسم ٥ يطلب في تبويب الإعدادات حقل «المدة الافتراضية». وجدول `settings` في `docs/schema.sql` فيه `hq_place` و`hq_map_url` و`logo_url` — ولا عمود للمدة.
- **السبب:** إضافة عمود تعديلٌ على المخطط — توقّف إجباري بالقسم ١١. والافتراضي حالياً ٩٠ دقيقة من `series.duration_min default 90`، ونموذج السلسلة الجديدة يبدأ به.
- **المهلة المتوقعة:** بُني الحقل معطّلاً مؤقّتاً (انظر القرار المسجَّل). يُحسم قبل النشر: إمّا عمود `default_duration_min smallint not null default 90 check (between 5 and 600)` بإذن صريح، وإمّا يبقى معطّلاً.

### 2026-08-31 عدّاد المحاولات لا يُشارَك بين نسخ الخادم
- **ماذا:** الحجب بعد خمس محاولات محفوظ في ذاكرة العملية (`lib/server/rate-limit.ts`). على Vercel قد تتعدّد النسخ، فيبدأ العدّ من الصفر في نسخة أخرى.
- **السبب:** البديل جدول في قاعدة البيانات، وهو تعديل على `schema.sql` — توقّف إجباري بالقسم ١١.
- **المهلة المتوقعة:** يُراجَع بعد النشر إن ظهرت محاولات تخمين فعلية. والحماية الحالية تكفي للتخمين اليدوي، وكلمة المرور ١٦ محرفاً عشوائياً.

### 2026-08-31 شاشة الدخول بلا نموذج معتمد
- **ماذا:** `/admin/login` لا نموذج بصري لها في `prototype/admin.html`. بُنيت بلبنات اللوحة نفسها: بطاقة `.panel` وحقل `.f` وزرّ `.btn.p`. وأُضيف `input[type='password']` إلى الورقة **بالقيم نفسها حرفاً بحرف** التي تحملها بقيّة الحقول — لأن قائمة النموذج تُنسّق text و date و time و number و url ولا password فيها.
- **السبب:** القسم ٥ يطلب الصفحة ولا يعطي شكلها، والنموذج لا يحويها.
- **المهلة المتوقعة:** تُعرض على صاحب المشروع مع بقية الفروق (§١١).

### 2026-08-31 معيار القبول ٢ لا يمكن إثباته أثناء وجود لقاء جارٍ
- **ماذا:** القسم ٩ يطلب أن يحوي نصّ `/` عبارة «اللقاء القادم» أو «لا لقاءات قادمة حالياً». والنموذج المعتمد يعرض في شريط البطل عبارة ثالثة — «يُبثّ الآن» — متى كان ثمّة لقاء جارٍ. فحين يجري لقاء لا تظهر أيٌّ من العبارتين.
- **السبب:** تعارض بين القسم ٩ والنموذج المعتمد. لا يجوز حسمه باجتهاد (§١١).
- **المهلة المتوقعة:** يُحسم قبل جولة معايير القبول: إمّا يُقرأ المعيار على أن «يُبثّ الآن» حالة ثالثة مشروعة، وإمّا يُختبر حين لا لقاء جارياً.

### 2026-08-31 نائب الشعار يعرض نصّاً داخلياً للزائر
- **ماذا:** ما دام `settings.logo_url` فارغاً، تعرض الترويسة نصّ النموذج «مساحة الشعار — يُرفع من اللوحة» لكل زائر.
- **السبب:** هو نصّ النموذج المعتمد حرفياً، وتغييره انحراف. ويختفي فور رفع الشعار.
- **المهلة المتوقعة:** يُرفع الشعار من تبويب الإعدادات (المرحلة ٩) قبل النشر. إن أُريد نصّ محايد للزائر قبل ذلك فهو قرار مستقل.

### 2026-08-31 رابط خرائط المقر وهمي
- **ماذا:** `settings.hq_map_url` قيمته `https://maps.google.com/?q=...` من `schema.sql`.
- **السبب:** المخطط زرعها كقيمة بديلة، وتغييرها الآن تعديلٌ لبيانات قائمة (توقّف إجباري بالقسم ١١).
- **المهلة المتوقعة:** تُضبط من تبويب الإعدادات بعد بناء المرحلة ٩. حتى ذلك، زرّ 📍 في عمود الوجهة يقود إلى رابط بلا معنى.

